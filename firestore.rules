rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data now flows through secured Next.js API routes that use
    // privileged server credentials. Direct browser access to Firestore is blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
